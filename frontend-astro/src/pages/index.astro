---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Redirecting to Companies...">
  <script>
    import { navigate } from 'astro:transitions/client';
    // Perform a soft client-side redirect so it stays within the SPA router
    navigate('/companies', { history: 'replace' });
  </script>
</Layout>
